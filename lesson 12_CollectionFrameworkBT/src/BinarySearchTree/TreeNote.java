package BinarySearchTree;

public class TreeNote<E> {
    protected E element;
    protected TreeNote<E> left;
    protected TreeNote<E> right;

    public TreeNote(E e) {
        element = e;
    }
}
